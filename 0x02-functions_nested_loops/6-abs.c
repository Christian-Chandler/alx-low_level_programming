#include "holberton.h"
/**
 * _abs - Computes the absolute value of a number.
 * @int: Type of number.
 * Return: Absolute value of the number.
 */
int _abs(int)
{
	int n;
	
	if (n >= 0)
		return (n);
	else
		return (-n);
}
